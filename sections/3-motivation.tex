%%%%%%%%%%%% Outline %%%%%%%%%%%%%%%%%%

% Section 3: Motivation

%%%
% message<motivation role: observations to design pressure>
% a real code-generation gap exists
% ordinary eBPF loses optimization-relevant semantics
% optimization profitability is deployment-dependent

% Section 3.1: Observation I: Real Code-Generation Gap

%%%
% message<same bytecode, weaker native code>
% stock kernel JIT below LLVM backend
% speed gap and code-size gap
% broad headroom
% system-level code-generation problem

% Section 3.2: Observation II: Lost Semantics Before Kernel Lowering

%%%
% message<rotate example as representative case>
% source-level semantic unit
% target ISA direct idiom
% ordinary eBPF expanded before kernel entry
% kernel JIT sees flattened sequence

%%%
% message<insight from the example>
% verifier-friendly canonical ISA
% poor optimization-facing representation
% one form serving two roles
% representation mismatch

% Section 3.3: Observation III: Deployment-Dependent Profitability

%%%
% message<legal rewrite, unstable profitability>
% workload-dependent benefit
% hardware-dependent benefit
% runtime-value-dependent benefit
% static always-on policy causes regressions

% Section 3.4: Existing Routes Still Miss One Side

%%%
% message<existing routes do not close both gaps>
% in-kernel JIT: closer to execution, higher trusted complexity
% pre-load optimizer: richer rewriting, no deployment-time facts
% native patching: post-load flexibility, no verifier-backed safety
% no route closes both mismatches

% Section 3.5: Design Pressure

%%%
% message<solution shape forced by the observations>
% richer optimization-facing bytecode
% deployment-time rewrite selection
% userspace policy, kernel mechanism
% re-verification through existing safety gate

%%%%%%%%%%%% Outline %%%%%%%%%%%%%%%%%%






% Section 2.2: Motivating Example: 64-bit Rotate

%%%
% message<a rotate is one semantic operation that the target ISA can express directly>
% motivating example: a 64-bit rotate in a hash-style computation
% at the source level, the computation is "rotate left by a constant"
% LLVM can still see that semantic operation
% on x86, LLVM lowers it directly to a rol instruction
% this is the code shape the target ISA already wants

%%%
% message<the current eBPF path erases that semantic structure before kernel lowering>
% ordinary eBPF has no first-class rotate instruction
% before the program reaches the kernel, the rotate has already been flattened into shifts and an or
% the kernel JIT therefore sees only the expanded ordinary-eBPF sequence
% it emits longer and less direct native code even though the ISA supports rotate directly
% Figure 2 should show this contrast; the text should only state the takeaway

%%%
% message<this example highlights a tension in the current pipeline>
% primitive bytecode is good for verification simplicity and portability
% but that same bytecode form does not preserve all optimization-relevant source structure
% once the structure is lost before the kernel, recovering the same code quality later requires substantially more sophistication in the in-kernel JIT
% the issue is therefore not that ordinary eBPF is a bad verification form
% the issue is that the verification form is also being asked to carry optimization intent

%%%
% message<key insight: optimization can use a different bytecode form than verification>
% keep ordinary eBPF as the kernel-facing bytecode for verification
% use a richer optimization-facing form, BPF', to preserve optimization-relevant semantics
% BPF' can be translated simply to ordinary eBPF for verification
% BPF' can also be lowered directly to efficient target code such as x86 rol
% this allows complex optimization on BPF' in userspace
% while reusing the simple verifier in kernel
% and keeping the kernel JIT simple

%%%%%%%%%%%% Outline %%%%%%%%%%%%%%%%%%

\section{Motivation}
\label{sec:motivation}



\subsection{Motivating Example: 64-bit Rotate}
\label{subsec:example}

The current compilation pipeline still leaves room for target-specific optimization. A simple example is a 64-bit rotate used in a hash-style computation. At the source level, the programmer expresses one semantic operation: rotate a 64-bit value left by a constant amount, as shown in Figure~\ref{fig:rotate-gap}. A userspace toolchain like LLVM optimizes this to use the accelerated \texttt{rol} instruction.

% [addressed] %note{hao: we can add a bar in this figure to separate the top and bottom and show clearly that the top part is the existing result, and the bottom is with kinsn, which requires one kinsn and two native insns only.}
\input{figures/sec-2-example}

Ordinary eBPF, however, does not contain an optimized bytecode instruction, so cannot preserve that rotate as a first-class instruction. By the time the kernel JIT receives the program, the operation has already been expanded into a sequence of shifts and an \texttt{or}. As a result, the native code emitted by the JIT can be longer, less direct and less performant than the native target ISA can support.

The example above illustrates a fundamental limitation of the current pipeline. Ordinary eBPF uses a primitive bytecode ISA, which keeps verifier reasoning simple and makes programs portable across architectures. But the same bytecode form does not preserve all optimization-relevant source semantics. Once operations (e.g., rotate or select) have been decomposed before entering the kernel, recovering equally efficient native code later would require substantially more analysis and target-specific logic in the in-kernel JIT. The issue, therefore, is that one bytecode form is being used both as a proof form for verification and as the input to native code generation.

% [addressed?] \note{hao: we can explain this example from three angles. For the compiler, kinsn extends the instruction choices; for the verifier, kinsn is lowered to its equivalent defs (maybe show this in the figure that the verifier still sees the 'ordinary BPF'), so no additional changes to the verifier's algorithm; for jit, it implements the machanim and lowers it to native. Three angles and callbacks to our design.}
%
% This example suggests that the ISA used to preserve optimization intent need not be the same ISA used for kernel verification. We realize this separation with \emph{kinsn} (Section~\ref{sec:kinsn}). For the compiler, kinsn adds explicit instruction choices such as rotate, instead of forcing those operations to be flattened early into ordinary eBPF. For the verifier, kinsn introduces no new checking algorithm: each site is first expanded into a semantics-equivalent ordinary-eBPF sequence, and the kernel continues to verify ordinary eBPF. For the JIT, the original kinsn site is lowered directly to native code while its semantics are still explicit, enabling efficient target instructions such as \texttt{rol}. The result is a cleaner split of responsibilities: rich optimization in userspace, existing verification in the kernel, and a simpler in-kernel JIT.

The key observation is that using one bytecode form both as the verifier-facing representation and as the input to native code generation creates conflicting design goals. Verification prefers a small, canonical ISA that the kernel can reason about uniformly across targets, whereas code generation benefits from retaining higher-level operations as explicit semantic units until late lowering. The key insight is therefore to decouple them: the bytecode form that preserves optimization intent need not be the same bytecode form that is submitted to the verifier. Section~\ref{sec:kinsn} introduces \emph{kinsn}, the bytecode abstraction that realizes this separation.
