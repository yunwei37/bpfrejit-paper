\begin{figure}[htbp]
  \centering
  \includegraphics[width=\linewidth]{figures/sec-2-example.drawio.pdf}
  \caption{A 64-bit rotate loses semantic structure as it moves through the current pipeline. The source-level rotate idiom is flattened into ordinary eBPF shifts and an \texttt{or}; the current x86 JIT therefore emits a longer shift/or sequence instead of the more direct \texttt{rol} idiom that the target ISA already supports.}
  \label{fig:rotate-gap}
\end{figure}
